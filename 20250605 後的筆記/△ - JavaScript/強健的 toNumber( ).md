---
date: 2025-11-07 18:34
aliases:
tags:
  - JavaScript
---
# Metadata
Status :: 🌱
Note Type :: 📰
Source URL :: {文章 URL}
Author :: {作者名稱}
Topics :: {筆記跟什麼主題有關，用 `[Topic],[Topic]` 格式}

---
# 連結筆記
#### 📑 [[]]

---

```javascript
function toNumber(value) {
    // 1. 檢查 null 或 undefined
    if (value === null || typeof value === 'undefined') {
        return null;
    }
    // 2. 如果是字串，先 trim 處理
    if (typeof value === 'string') {
        value = value.trim();
        if (value === '') {
            return null; // 空字串會在這裡回傳
        }
    }
    // 3. 轉換為數字
    var num = Number(value);
    // 4. 檢查轉換結果是否為 NaN
    return isNaN(num) ? null : num;
}
```

|**傳入的值 (Input)**|**函式步驟**|**num 的值 (第3步)**|**isNaN(num) (第4步)**|**最終結果 (Output)**|
|---|---|---|---|---|
|**`false`**|2. (跳過) → 3.|`Number(false)` → `0`|`isNaN(0)` → `false`|**`0`**|
|**`0`**|2. (跳過) → 3.|`Number(0)` → `0`|`isNaN(0)` → `false`|**`0`**|
|**`-0`**|2. (跳過) → 3.|`Number(-0)` → `-0`|`isNaN(-0)` → `false`|**`-0`**|
|**`0n`**|2. (跳過) → 3.|`Number(0n)` → `0`|`isNaN(0)` → `false`|**`0`**|
|**`""`**|2. (執行)|(未執行)|(未執行)|**`null`**|
|**`''`**|2. (執行)|(未執行)|(未執行)|**`null`**|
|**` `` `**|2. (執行)|(未執行)|(未執行)|**`null`**|
|**`null`**|1. (執行)|(未執行)|(未執行)|**`null`**|
|**`undefined`**|1. (執行)|(未執行)|(未執行)|**`null`**|
|**`NaN`**|2. (跳過) → 3.|`Number(NaN)` → `NaN`|`isNaN(NaN)` → `true`|**`null`**|

### 結論

這個 `toNumber` 函式非常強健（robust），它成功地將「空的」或「無效的」值 (`""`, `null`, `undefined`, `NaN`) 都轉換成了 `null`，同時正確地保留了「零值」 (`false`, `0`, `-0`, `0n`) 為數字 `0`（或 `-0`）。