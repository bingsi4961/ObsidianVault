---
date: 2025-11-06 14:37
aliases:
tags:
  - jQuery
---
# Metadata
Status :: 🌱
Note Type :: 📰
Source URL :: {文章 URL}
Author :: {作者名稱}
Topics :: {筆記跟什麼主題有關，用 `[Topic],[Topic]` 格式}

---
# 連結筆記
#### 📑 [[]]

---
### 基本用法

```javascript
var qryObj = { name: "John", age: 30 };
$.param(qryObj);
// 輸出: "name=John&age=30"

$.param({ name: "John", age: 30 });
// 輸出: "name=John&age=30"
```

### 處理數組

不加參數時的默認行為：
這種是 PHP 或 Rails 等框架偏好的格式，也是 jQuery 的默認值。但 **不是** ASP.NET 預設綁定查詢字串的格式。

```javascript
$.param({ colors: ["red", "green", "blue"] });
// 輸出: "colors%5B%5D=red&colors%5B%5D=green&colors%5B%5D=blue"
// 解碼後: "colors[]=red&colors[]=green&colors[]=blue"
```

使用第二個參數 `true` 來處理傳統格式 (Traditional Style)：
🚨 這種格式（多個同名鍵）**正是** ASP.NET 模型綁定器 (Model Binder) 所預期的格式。

```javascript
$.param({ colors: ["red", "green", "blue"] }, true);
// 輸出: "colors=red&colors=green&colors=blue"
```

### 在您的代碼中

- 後端 C# 程式碼
```csharp
// .Net Framework 或 .Net Core 中的 Controller Action
// 它期望接收一個名為 "check" 的字串陣列
public FileResult ExportMemberList(string[] check) 
{ 
    // 此時 "check" 參數將會是 ["1", "3", "5"]
    // ...
}
```

- 前端 JavaScript 程式碼
```javascript
let checkedValues = ["1", "3", "5"]; // 假設這是從 checkbox 獲取的值

// *** 正確的做法 (使用傳統模式) ***
let urlParams = $.param({"check": checkedValues}, true);
// 產生: "check=1&check=3&check=5" 
// ASP.NET 看到多個同名的 'check' 參數，會自動將它們 
// 彙總成一個 string[] 陣列。

// 組合 URL
window.location.href = '@Url.Action("ExportMemberList")?' + urlParams;
```